"""
Developer tools for looking at the real window - the thing a Tkinter app cannot
show you from the code alone.

    python tools/uishots/run.py --out shots --themes warm premium

Launches the real application against a throwaway install (never your novels or
your settings), and saves PNGs of the main window and the tool windows, plus a
report of anything that does not fit its window. See `run.py`.
"""
